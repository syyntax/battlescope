version: '3.8'

services:
  battlescope:
    build: .
    container_name: battlescope
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
